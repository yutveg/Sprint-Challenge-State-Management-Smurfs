import React from "react";
import { connect } from "react-redux";

const SmurfList = props => {
  return <div></div>;
};

export default connect(mapStateToProps, {
  /* Get Smurfs Action, Add Smurf Action */
})(SmurfList);
